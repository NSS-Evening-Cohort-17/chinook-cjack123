SELECT 
    COUNT(InvoiceId) num_of_line_items
FROM InvoiceLine
WHERE InvoiceId LIKE 37


-- WHERE InvoiceLineId like 37

-- InvoiceID
-- Quantity





-- Looking at the InvoiceLine table, 
-- provide a query that COUNTs the 
-- number of line items for Invoice ID 37.